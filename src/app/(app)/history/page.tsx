import { HistoryWorkspace } from '@/components/reporting/HistoryWorkspace';

export const metadata = { title: 'Past purchases' };

export default function HistoryPage() {
  return <HistoryWorkspace />;
}
