import { InsightsWorkspace } from '@/components/reporting/InsightsWorkspace';

export const metadata = { title: 'Savings and prices' };

export default function InsightsPage() {
  return <InsightsWorkspace />;
}
