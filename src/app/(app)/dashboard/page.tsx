import { OverviewWorkspace } from '@/components/overview/OverviewWorkspace';

export const metadata = { title: 'Overview · QuotePlate' };

export default function DashboardPage() {
  return <OverviewWorkspace />;
}
